# Notification App Backend

This is a frontend-track submission, so no backend service is required in this folder.

The React frontend consumes the provided AffordMed notification API and log API directly, using the reusable logging middleware kept at the repository root.
